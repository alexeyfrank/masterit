%h1 Редактирование настроек сайта

= simple_form_for(:settings, url: admin_settings_path) do |f|
  - @settings.each do |k, v|
    = f.input k, input_html: { value: v }
  = f.submit class: 'btn btn-primary'